# Backend

Install dependencies:
```
cd backend
npm install
```

Create `.env` from `.env.example` and set `MONGO_URI` and `JWT_SECRET`.

Run dev:
```
npm run dev
# or
npm start
```

Seed example data:
```
npm run seed
```
